<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://Oolong-Tea1.github.io</id>
    <title>OolongTea</title>
    <updated>2022-05-19T14:25:20.548Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://Oolong-Tea1.github.io"/>
    <link rel="self" href="https://Oolong-Tea1.github.io/atom.xml"/>
    <subtitle>温故而知新</subtitle>
    <logo>https://Oolong-Tea1.github.io/images/avatar.png</logo>
    <icon>https://Oolong-Tea1.github.io/favicon.ico</icon>
    <rights>All rights reserved 2022, OolongTea</rights>
    <entry>
        <title type="html"><![CDATA[Javascript学习日记：基础语法、对象、BOM、DOM、事件监听]]></title>
        <id>https://Oolong-Tea1.github.io/post/javascript-xue-xi-ri-ji-ji-chu-yu-fa-dui-xiang-bomdomshi-jian-jian-ting/</id>
        <link href="https://Oolong-Tea1.github.io/post/javascript-xue-xi-ri-ji-ji-chu-yu-fa-dui-xiang-bomdomshi-jian-jian-ting/">
        </link>
        <updated>2022-05-19T14:22:36.000Z</updated>
        <content type="html"><![CDATA[<h3 id="一-基础语法">一、基础语法</h3>
<h4 id="如何引入">如何引入</h4>
<p>​		Javascript有两种引入方式，</p>
<h5 id="第一种直接在html文件中加入script标签">第一种：直接在html文件中加入script标签</h5>
<p>​				像这样子</p>
<pre><code>&lt;body&gt;

&lt;script&gt;
    alert(&quot;Hello Javascript&quot;);
&lt;/script&gt;


&lt;/body&gt;
</code></pre>
<h5 id="第二种在外部包写js文件在html文件中导入js文件">第二种：在外部包写js文件，在html文件中导入js文件</h5>
<pre><code>&lt;script src=&quot;../js/Demo.js&quot;&gt;
    
&lt;/script&gt;
</code></pre>
<p>​			<strong>Tip：导入外部js的文件中不能包含<script>标签</strong></p>
<p>​						<strong>且导入时html文件中的<script>标签不能自闭合</strong></p>
<p>​	<strong>一般</strong>会把<script>标签<strong>放在<body>标签的最下面</strong>，js是脚本语言，所以为了用户的体验感受，会优先加载出页面的	基本内容，再加载动态信息，所以优先把<script>标签放在<body>标签的最下面。但是如果网页的js文件是全局定义且后期不怎么修改的，<strong>也可以放在<html>标签中</strong>。</p>
<h3 id="二基本语法">二：基本语法</h3>
<h4 id="1-输出语句">1、输出语句</h4>
<pre><code>window.alert(&quot;Hello Javascript&quot;);//写入警告框
document.write(&quot;Hello Javascript&quot;);//写入Html
console.log(&quot;Hello Javascript&quot;);//写入控制台
</code></pre>
<figure data-type="image" tabindex="1"><img src="C:%5CUsers%5Cmr%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20220519190126871.png" alt="image-20220519190126871" loading="lazy"></figure>
<figure data-type="image" tabindex="2"><img src="C:%5CUsers%5Cmr%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20220519190140451.png" alt="image-20220519190140451" loading="lazy"></figure>
<figure data-type="image" tabindex="3"><img src="C:%5CUsers%5Cmr%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20220519190149270.png" alt="image-20220519190149270" loading="lazy"></figure>
<p>​		Tip：<script> 中语句结尾的分号可以删掉。</p>
<h4 id="2-变量">2、变量：</h4>
<p>​	2.1、js里面有<strong>var</strong>来申明变量，var变量可以<strong>存放不同类型的值</strong>。</p>
<p>​	2.2、变量名的规则与<strong>java一致</strong>。</p>
<p>​	2.3、ECMAscript6 新增了<strong>let</strong>关键字来定义变量，用法类似var，声明的变量只在let关键字所在的代码快生效，且<strong>不			允许重复声明</strong>。</p>
<p>​	2.4、ECMAscript6 新增了<strong>const</strong>关键字，用来声明一个只读的变量，一旦声明，变量的值就<strong>不能改变</strong>。</p>
<h4 id="3-数据类型">3、数据类型：</h4>
<p>​	3.1、原始类型</p>
<ul>
<li>number</li>
<li>string</li>
<li>boolean</li>
<li>null</li>
<li>undefined</li>
</ul>
<p>​		Tip:可以使用<strong>typeof运算符</strong>获取数据类型</p>
<h4 id="4-运算符">4、运算符</h4>
<p>​	区分“<mark>”和“</mark>=”：</p>
<p>​		“==”：判断类型是否一致，如果不一致就进行类型转换，再对值进行比较</p>
<p>​		“===”：判断类型是否一致，如果不一致直接返回false，再对值进行比较</p>
<h4 id="5-类型转换">5、类型转换：</h4>
<h5 id="51-其他类型转为number">5.1、其他类型转为Number</h5>
<ul>
<li>
<p>string ：按照字符串的字面值，转为数字，如果字符串的字面值不是数字，则返回NaN，一般使用					parseInt</p>
<ul>
<li>boolean ：true转为1，其他转为0</li>
</ul>
</li>
</ul>
<h5 id="52-其他类型转为boolean">5.2、其他类型转为boolean</h5>
<ul>
<li>number:0和NaN转为false，其他转为true</li>
<li>string:空字符串转为false，其他转为true</li>
<li>null:false</li>
<li>undefined:false</li>
</ul>
<h4 id="6-流程控制语句">6、流程控制语句</h4>
<pre><code>//if
var count = 3;
if (count == 3){
    alert(count);
}

//switch
var num = 3;
switch (num){
    case 1:{
        alert('今天是星期一')
        break;
    }
    case 2:{
        alert('今天是星期二')
        break;
    }case 3:{
        alert('今天是星期三')
        break;
    }case 4:{
        alert('今天是星期四')
        break;
    }case 5:{
        alert('今天是星期五')
        break;
    }case 6:{
        alert('今天是星期六')
        break;
    }case 7:{
        alert('今天是星期日')
        break;
    }
    default:alert('输入信息有误');
}

//for
var sum = 0;
for (let i = 0 ; i &lt;= 100 ; i++){
    sum += i;
}
alert(sum);

//while
var sum = 0;
var i = 1;
while (i &lt;= 100){
    sum += i;
    i++;
}
alert(sum);

//do...while
var sum = 0;
var i = 1;
do {
    sum += i;
    i++;
}while (i &lt;= 100);
alert(sum);
</code></pre>
<h4 id="7-函数">7、函数</h4>
<h5 id="71-定义函数">7.1、定义函数</h5>
<pre><code>function add(a,b){
    return a+b;
}
let result = add(1,2)
</code></pre>
<p>注意：<strong>形式参数不需要类型</strong>，返回值也不需要定义类型，可以在函数内部<strong>直接使用return</strong>即可</p>
<pre><code>var add = function (a,b){
    return a+b;
}
// let result = add(1,2)
//在js中调用函数可以传入任意个参数，传进几个就使用几个，超出定义时的参数个数，超过的部分也不会干涉原本的程序运行。
let result = add(1,2,3,4)
</code></pre>
<h3 id="三-对象">三、对象</h3>
<p>​	参考手册：<a href="https://www.w3school.com.cn/js/js_objects.asp">JavaScript 对象 (w3school.com.cn)</a></p>
<figure data-type="image" tabindex="4"><img src="C:%5CUsers%5Cmr%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20220519201350181.png" alt="image-20220519201350181" loading="lazy"></figure>
<h4 id="31-array">3.1、Array</h4>
<h5 id="a-定义和访问">a、定义和访问：</h5>
<pre><code>//方式一
var arr = new Array(1,2,3)
alert(arr[0]);
//方式二
var arr2 = [1,2,3];
alert(arr[0]);
arr2[0] = 10;
alert(arr[0]);
</code></pre>
<h5 id="b-属性length">b、属性：length</h5>
<pre><code>var arr4 = [1,2,3];
for (let i = 0; i &lt; arr4.length; i++) {
    alert(arr4[i])
}
</code></pre>
<h5 id="c-方法">c、方法</h5>
<ul>
<li>
<p>push</p>
<pre><code>arr4.push(10);
alert(arr4);
</code></pre>
</li>
<li>
<p>splice</p>
<pre><code>arr4.splice(0,1);//从0开始，删除一个
alert(arr4)
</code></pre>
</li>
</ul>
<h4 id="32-string">3.2、String</h4>
<h5 id="a-定义和方法">a、定义和方法</h5>
<pre><code>var str1 = new String('abc');

var str2 = 'abc';
var str3 = &quot;abc&quot;;
</code></pre>
<h5 id="b-属性">b、属性</h5>
<pre><code>alert(str3.length);
</code></pre>
<h5 id="c-方法-2">c、方法</h5>
<pre><code>var str4 = '    abc     ';
alert(1 + str4 + 1);
alert(1 + str4.trim() + 1);
</code></pre>
<h4 id="33-其他对象">3.3、其他对象</h4>
<pre><code>var person = {
  name:&quot;ZhangSan&quot;,
    age:23,
    eat:function (){
      alert(&quot;吃饭&quot;);
    }
}

alert(person.name);
alert(person.age);
person.eat()
</code></pre>
<h3 id="四-bom">四、BOM</h3>
<h4 id="41-window">4.1、Window</h4>
<ul>
<li>方法</li>
</ul>
<pre><code>//alert
window.alert('abc');
alert('abc');

//confirm
var flag = confirm('确认删除？');
alert(flag)//确定返回true，取消返回false
if (flag){
    //删除逻辑
}else {

}

//定时器
setTimeout(function (){
    alert('hh');
},3000);
setInterval(function (){
    alert('hhh');
},2000);
</code></pre>
<pre><code>/**
    setTimeout(function,毫秒值) :在一定的时间间隔后执行一个function，只执行一次
    setInterval(function,毫秒值) :在一定的时间间隔后执行一个function，循环执行
 */
</code></pre>
<p>​</p>
<h4 id="42-navigator浏览器对象">4.2、Navigator：浏览器对象</h4>
<h4 id="43-screen屏幕对象">4.3、Screen：屏幕对象</h4>
<h4 id="44-history历史对象">4.4、History：历史对象</h4>
<h4 id="45-location地址对象">4.5、Location：地址对象</h4>
<pre><code>alert('要跳转了')
location.href = &quot;oolong-tea1.github.io&quot;
</code></pre>
<h3 id="五-dom">五、DOM</h3>
<p>​		：能对HTML文件进行修改</p>
<h4 id="51-document整个文档对象">5.1、Document：整个文档对象</h4>
<h4 id="52-element元素对象">5.2、Element：元素对象</h4>
<h4 id="53-attribute属性对象">5.3、Attribute：属性对象</h4>
<h4 id="54-text文本对象">5.4、Text：文本对象</h4>
<h4 id="55-comment注释对象">5.5、Comment：注释对象</h4>
<h5 id="方法">：方法</h5>
<pre><code>//1、getElementById:根据id属性值获取，返回一个Element对象
var img = document.getElementsByClassName(&quot;light&quot;);
alert(img);
img.src = '../images/sun.jpg';

//2、getElementByTabName:根据标签名称获取，返回Element对象数组
var divs = document.getElementsByTagName(&quot;div&quot;);
/*
    style:设置css样式
    innerHTML:设置元素内容
 */
for (let i = 0; i &lt; divs.length; i++) {
    divs[i].style.color = 'red';
    divs[i].innerHTML = 'HH';
}

//3、getElementByName:根据name属性值获取，返回Element对象数组
var hobbies = document.getElementsByName(&quot;hobby&quot;);
for (let i = 0; i &lt; hobbies.length; i++) {
    alert(hobbies[i]);
}

//4、getElementByClassName:根据Class属性值获取，返回Element对象数组
var clas = document.getElementsByClassName(&quot;cls&quot;);
for (let i = 0; i &lt; clas.length; i++) {
    alert(clas[i]);
}
</code></pre>
<h3 id="六-事件监听">六、事件监听</h3>
<h4 id="61-事件绑定">6.1、事件绑定</h4>
<p>​				两种方法：</p>
<pre><code>&lt;script&gt;
	//方法一：
    function on(){
        alert(&quot;点我干嘛&quot;)
    }

	//方法二：
    document.getElementById(&quot;btn&quot;).onclick = function (){
        alert(&quot;点我干什么&quot;)
    }
&lt;/script&gt;
</code></pre>
<h4 id="62-常见事件">6.2、常见事件</h4>
<p>​			参考手册：<a href="https://www.w3school.com.cn/jsref/dom_obj_event.asp">HTML DOM 事件 (w3school.com.cn)</a></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[MayBatis学习日记：增删改查]]></title>
        <id>https://Oolong-Tea1.github.io/post/maybatis-xue-xi-ri-ji-zeng-shan-gai-cha/</id>
        <link href="https://Oolong-Tea1.github.io/post/maybatis-xue-xi-ri-ji-zeng-shan-gai-cha/">
        </link>
        <updated>2022-05-18T10:20:45.000Z</updated>
        <content type="html"><![CDATA[<h3 id="一-查">一、查：</h3>
<h4 id="1-resultmap">1、resultMap:</h4>
<pre><code class="language-java">&lt;mapper namespace=&quot;Mapper.BrandMapper&quot;&gt;
    &lt;select id=&quot;selectAll&quot; resultType=&quot;pojo.Brand&quot;&gt;
        select * from Brand
    &lt;/select&gt;
&lt;/mapper&gt;
</code></pre>
<p>一般的查询语句是像上面这样子写的，但是在实际应用中，如果根据某个字段查询表，用resultType的话就只能用pojo.user类里面定义的变量名，有时候会跟表里面的字段名不一致，如果用resultType参数的话，则需在查询时加上这串代码</p>
<pre><code class="language-java">&lt;select id=&quot;selectAll&quot; resultType=&quot;pojo.Brand&quot;&gt;
    select id , brand_Name as brandName , company_Name as companyName , ordered , description , status
    from brand
&lt;/select&gt;
</code></pre>
<p>但是这样子的话也是很不方便，毕竟现实中我们不可能就查这个表一次，我们需要用多条查询语句来满足需求，如果用这种方法写的话那每条语句都得这样子加上as，代码很复杂也很难看。</p>
<p>Mybatis中提供了resultMap元素，示例代码如下：</p>
<pre><code class="language-java">&lt;resultMap id=&quot;BrandResultMap&quot; type=&quot;pojo.Brand&quot;&gt;
    &lt;!--
        id:完成主键字段的映射
            column:表的列名
            property:实体类的属性名
        result:完成一般字段的映射
            column:表的列名
            property:实体类的属性名
     --&gt;
    &lt;result column=&quot;brand_Name&quot; property=&quot;brandName&quot;/&gt;
    &lt;result column=&quot;company_Name&quot; property=&quot;companyName&quot;/&gt;
&lt;/resultMap&gt;
</code></pre>
<p>像这样子写后我们在查询语句中就可以直接用brandName和companyName来代替表中的字段brand_Name和company_Name了。</p>
<p>将查询语句中的resultType改成resultMap就可以了。</p>
<pre><code class="language-java">&lt;select id=&quot;selectAll&quot; resultMap=&quot;BrandResultMap&quot;&gt;
    select *
    from brand
&lt;/select&gt;
</code></pre>
<h4 id="2-参数占位符">2、参数占位符：</h4>
<p>有查询就肯定会需要条件查询，条件查询就离不开参数占位符。</p>
<p>在Mybatis有<strong>两种</strong>参数占位符，一种是**#{}<strong>，一种是</strong>${}**。</p>
<pre><code>#{}：会将其替换为？ 防止sql注入
${}：不替换拼sql，会存在sql注入问题
</code></pre>
<p><strong>使用的时机？</strong></p>
<pre><code>*参数传递的时候用#{}
*表名或列名不固定的时候用：${}，会存在sql注入问题
</code></pre>
<p>如果表中有<strong>特殊字符</strong>怎么处理，Mybatis提供了两种解决方法，一种是使用<strong>转义字符</strong>，一种是使用<strong>CDATA区</strong>。</p>
<p>例如：</p>
<pre><code>&lt;select id=&quot;selectbyidBrands&quot; resultMap=&quot;BrandResultMap&quot;&gt;
    select *
    from brand where id &lt; #{id};
&lt;/select&gt;
</code></pre>
<p>在where后面使用“&lt;”号，系统会报错<br>
<img src="https://Oolong-Tea1.github.io/post-images/1652869523752.png" alt="" loading="lazy"></p>
<p>这时候就可以用上转义字符</p>
<figure data-type="image" tabindex="1"><img src="https://Oolong-Tea1.github.io/post-images/1652869543860.png" alt="" loading="lazy"></figure>
<p>现在这样子就不报错了。</p>
<p>第二种是用CDATA，如下图</p>
<figure data-type="image" tabindex="2"><img src="https://Oolong-Tea1.github.io/post-images/1652869557792.png" alt="" loading="lazy"></figure>
<p>将代码改成这样，可以看到，idea也没有给我们报错。</p>
<h4 id="3-判断语句">3、判断语句：</h4>
<h5 id="31-where">3.1、where</h5>
<pre><code>&lt;select id=&quot;selectByCondition&quot; resultMap=&quot;BrandResultMap&quot;&gt;
        select * from brand
#         where 1=1 &lt;!--用恒等式让&lt;if&gt;里面的语句无论成不成立都不会让sql语句出现问题 可以用&lt;where&gt;代替where关键字 --&gt;
        &lt;where&gt;
            &lt;if test=&quot;status != null&quot;&gt;
                and status = #{status}
            &lt;/if&gt;
            &lt;if test=&quot;brandName != null and brandName != '' &quot;&gt;
                and brand_Name like #{brandName}
            &lt;/if&gt;
            &lt;if test=&quot;companyName != null and companyName != '' &quot;&gt; &lt;!--因为这里的companyName是传输进来的值来判断的，所以不能用company_Name --&gt;
                and company_Name like #{companyName}
            &lt;/if&gt;
        &lt;/where&gt;


    &lt;/select&gt;
</code></pre>
<h5 id="32-choose">3.2、choose</h5>
<pre><code>&lt;select id=&quot;selectByConditionSingle&quot; resultMap=&quot;BrandResultMap&quot;&gt;
    select * from brand
    where
    &lt;choose&gt; &lt;!--相当于switch --&gt;
        &lt;when test=&quot;status != null&quot;&gt; &lt;!--相当于switch中的case --&gt;
            status = #{status}
        &lt;/when&gt;
        &lt;when test=&quot;brandName != null and brandName != ''&quot;&gt;
            brand_Name like #{brandName}
        &lt;/when&gt;
        &lt;when test=&quot;companyName != null and brandName != ''&quot;&gt;
            company_Name like #{companyName}
        &lt;/when&gt;
        &lt;otherwise&gt;
            1 = 1 &lt;!-- 因为前面三个参数都有可能为空，所以需要有一个恒等式来支撑sql运行不报错--&gt;
        &lt;/otherwise&gt;
    &lt;/choose&gt;


&lt;/select&gt;
</code></pre>
<h3 id="二增">二：增</h3>
<pre><code>&lt;insert id=&quot;add&quot; useGeneratedKeys=&quot;true&quot; keyProperty=&quot;id&quot;&gt;
    insert into brand(brand_Name, company_Name, ordered, description, status)
    VALUE (#{brandName},#{companyName},#{ordered},#{description},#{status})

&lt;/insert&gt;
</code></pre>
<pre><code>@Test
public void add() throws Exception{
    int status = 1;
    String companyName = &quot;abc&quot;;
    String brandName = &quot;ABC&quot;;
    int ordered = 2;
    String description = &quot;abcdedf&quot;;

    Brand brand = new Brand();
    brand.setStatus(status);
    brand.setBrandName(brandName);
    brand.setCompanyName(companyName);
    brand.setDescription(description);
    brand.setOrdered(ordered);


    String resource = &quot;mybatis-config.xml&quot;;
    InputStream inputStream = Resources.getResourceAsStream(resource);
    SqlSessionFactory sqlSessionFactory = new SqlSessionFactoryBuilder().build(inputStream);

    SqlSession sqlSession = sqlSessionFactory.openSession();

    BrandMapper brandMapper = sqlSession.getMapper(Mapper.BrandMapper.class);

    int row = brandMapper.add(brand);


    System.out.println(row);

    sqlSession.close();
}
</code></pre>
<figure data-type="image" tabindex="3"><img src="https://Oolong-Tea1.github.io/post-images/1652869585227.png" alt="" loading="lazy"></figure>
<p>上面是我表中的数据，以下是我运行完的结果。</p>
<figure data-type="image" tabindex="4"><img src="https://Oolong-Tea1.github.io/post-images/1652869604530.png" alt="" loading="lazy"></figure>
<p>可以看到表中的数据刷新完没有变化。</p>
<p>这个原因我在网上查了一下，原因是<strong>mybatis不会自动帮咱们提交事务</strong>，虽然程序运行成功了，但是<strong>事务没有提交上</strong>去，表中自然就没有数据，那我们<strong>只需要提交一下事务</strong>不就可以了。</p>
<pre><code>@Test
public void add() throws Exception{
    int status = 1;
    String companyName = &quot;abc&quot;;
    String brandName = &quot;ABC&quot;;
    int ordered = 2;
    String description = &quot;abcdedf&quot;;

    Brand brand = new Brand();
    brand.setStatus(status);
    brand.setBrandName(brandName);
    brand.setCompanyName(companyName);
    brand.setDescription(description);
    brand.setOrdered(ordered);


    String resource = &quot;mybatis-config.xml&quot;;
    InputStream inputStream = Resources.getResourceAsStream(resource);
    SqlSessionFactory sqlSessionFactory = new SqlSessionFactoryBuilder().build(inputStream);

    SqlSession sqlSession = sqlSessionFactory.openSession();

    BrandMapper brandMapper = sqlSession.getMapper(Mapper.BrandMapper.class);


    int row = brandMapper.add(brand);
    //提交事务
    sqlSession.commit();
    
    System.out.println(row);

    sqlSession.close();
}
</code></pre>
<figure data-type="image" tabindex="5"><img src="https://Oolong-Tea1.github.io/post-images/1652869617275.png" alt="" loading="lazy"></figure>
<p>运行时刷新完表就有数据了。</p>
<h3 id="三改">三：改</h3>
<pre><code>&lt;update id=&quot;update&quot;&gt;
    update brand
    &lt;set&gt;
        &lt;if test=&quot;status != null&quot;&gt;
            status = #{status},
        &lt;/if&gt;
        &lt;if test=&quot;brandName != null and brandName != '' &quot;&gt;
            brand_Name like #{brandName},
        &lt;/if&gt;
        &lt;if test=&quot;companyName != null and companyName != '' &quot;&gt;
        &lt;!--因为这里的companyName是传输进来的值来判断的，所以不能用company_Name --&gt;
            company_Name like #{companyName},
        &lt;/if&gt;
        &lt;if test=&quot;status != null&quot;&gt;
            ordered = #{ordered},
        &lt;/if&gt;
        &lt;if test=&quot;brandName != null and brandName != '' &quot;&gt;
            description = #{description}
        &lt;/if&gt;
    &lt;/set&gt;
    where id = #{id}
&lt;/update&gt;
</code></pre>
<p>如果<strong>不加<if><strong>元素会产生</strong>将数据改成null</strong>的情况。</p>
<p>这里<strong>用<set>元素</strong>，可以<strong>避免</strong>当最后一个判定字段为null，前一个字段不为null时sql语句中的逗号<strong>引起报错</strong></p>
<h3 id="四删">四：删：</h3>
<h4 id="根据id删除数据">根据id删除数据。</h4>
<pre><code>public int deleteByIds(int[] ids);
</code></pre>
<pre><code>&lt;delete id=&quot;deleteByIdInt&quot;&gt;
    delete from brand where id = #{id};
&lt;/delete&gt;
</code></pre>
<pre><code>@Test
public void deleteById() throws Exception{
    int id = 1;

    String resource = &quot;mybatis-config.xml&quot;;
    InputStream inputStream = Resources.getResourceAsStream(resource);
    SqlSessionFactory sqlSessionFactory = new SqlSessionFactoryBuilder().build(inputStream);

    SqlSession sqlSession = sqlSessionFactory.openSession(true);

    BrandMapper brandMapper = sqlSession.getMapper(Mapper.BrandMapper.class);

    int row = brandMapper.deleteByIdInt(id);

    System.out.println(row);

    sqlSession.close();
}
</code></pre>
<pre><code>&lt;!--
 Mybatis会将数组参数封装成一个Map集合
    默认情况下foreach元素里面的collection参数的值为array，
    但是可以在mapper文件中添加@param注解变成你想要的字段。
 --&gt;
</code></pre>
<p>根据上面的代码运行完报了错，可以看到报错提醒是：</p>
<p>Parameter 'ids' not found. Available parameters are [array, arg0]</p>
<p><img src="https://Oolong-Tea1.github.io/post-images/1652869636338.png" alt="" loading="lazy"><br>
原来是因为忘了<strong>加上@Param注解</strong>，加上注解再跑一次</p>
<figure data-type="image" tabindex="6"><img src="https://Oolong-Tea1.github.io/post-images/1652869648156.png" alt="" loading="lazy"></figure>
<p>加上注解后就成功跑起来了，没有改变的行数是因为表里已经没有这些数据了，所以返回0.</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Mybatis学习日记：解决：找不到配置文件问题]]></title>
        <id>https://Oolong-Tea1.github.io/post/mybatis-xue-xi-ri-ji-jie-jue-zhao-bu-dao-pei-zhi-wen-jian-wen-ti/</id>
        <link href="https://Oolong-Tea1.github.io/post/mybatis-xue-xi-ri-ji-jie-jue-zhao-bu-dao-pei-zhi-wen-jian-wen-ti/">
        </link>
        <updated>2022-05-17T03:13:58.000Z</updated>
        <content type="html"><![CDATA[<p>今天在配置Mybatis环境时遇到了些问题，主要还是找不到配置文件的问题</p>
<p>!<img src="https://Oolong-Tea1.github.io/post-images/1652768715842.png" alt="" loading="lazy"></p>
<p>上面是我的文件目录</p>
<h2 id="问题一找不到主要配置文件-mybatis-configxml">问题一：找不到主要配置文件 mybatis-config.xml</h2>
<figure data-type="image" tabindex="1"><img src="https://Oolong-Tea1.github.io/post-images/1652769052908.png" alt="" loading="lazy"></figure>
<p>上图使用了绝对路径来获取配置文件，从图中可以看到我们并不能这样子获取到配置文件</p>
<p><strong>那我们应该怎么解决呢？</strong></p>
<p>只要把绝对路径改成文件名，也就是&quot;<strong>mybatis-config.xm</strong>l&quot;即可<img src="https://Oolong-Tea1.github.io/post-images/1652769075160.png" alt="" loading="lazy"></p>
<h2 id="问题二找不到映射文件">问题二：找不到映射文件</h2>
<h3 id="原因一mybatis-configxml配置文件中的mapper-resource配置映射文件时路径写错了">原因一：mybatis-config.xml配置文件中的**<mapper resource>**配置映射文件时路径写错了																		<img src="https://Oolong-Tea1.github.io/post-images/1652769115078.png" alt="" loading="lazy"></h3>
<p>​		查了一下Mybatis官网，将路径改成下面这种形式就好了</p>
<p><img src="https://Oolong-Tea1.github.io/post-images/1652769151258.png" alt="" loading="lazy"><br>
<img src="https://Oolong-Tea1.github.io/post-images/1652769161589.png" alt="" loading="lazy"></p>
<h3 id="原因二映射文件和mapper文件不在同一个资源包下面">原因二：映射文件和mapper文件不在同一个资源包下面</h3>
<p>####解决一：可以将xml文件和mapper文件放在java文件夹下的Mapper包中，如下图所示</p>
<p>​							<img src="https://Oolong-Tea1.github.io/post-images/1652769240523.png" alt="" loading="lazy"></p>
<p>再在pom.xml文件中添加上这串配置</p>
<pre><code class="language-Java">&lt;build&gt;
    &lt;resources&gt;
        &lt;resource&gt;
            &lt;directory&gt;src/main/java&lt;/directory&gt;
            &lt;includes&gt;
                &lt;include&gt;**/*.xml&lt;/include&gt;
            &lt;/includes&gt;
        &lt;/resource&gt;
    &lt;/resources&gt;
&lt;/build&gt;
</code></pre>
<p>​	就可以成功运行了</p>
<h4 id="解决二将映射文件放到resource包下">解决二：将映射文件放到resource包下</h4>
<p>​		将映射文件和mapper文件放在一起虽然能解决问题，但是也存在问题。就是如果在工程中，这样子并不美观且很难维护。</p>
<p>​		在resource包下创建一个目录，目录名要与java包下存放mapper文件的包名一致，像我下面这样子。<br>
<img src="https://Oolong-Tea1.github.io/post-images/1652769382448.png" alt="" loading="lazy"><br>
​				Tip：可以在target包下的classes下查看mapper文件和映射文件有没有在同一个目录下。<br>
<img src="https://Oolong-Tea1.github.io/post-images/1652769340896.png" alt="" loading="lazy"></p>
<p>​			如果存放的mapper文件的包名有xxx.xxx.xxx类似的结构，需要在创建目录时用以下格式创建</p>
<figure data-type="image" tabindex="2"><img src="https://Oolong-Tea1.github.io/post-images/1652769312683.png" alt="" loading="lazy"></figure>
]]></content>
    </entry>
</feed>